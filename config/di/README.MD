In this directory could be two files:

* **services.php**

    File with application services definitions
    
    
* **components.php**

    File with component definitions. Returns array of ServiceProviders, one for each used component.
